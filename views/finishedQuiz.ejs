<!DOCTYPE html>
<head>
    <title></title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>

    <h4>You socred <%= score %> out of <%= quizLength %></h4>
    <% if (myMinutes === 0) { %>
        <h3>You finished the quiz in <%= mySeconds%> seconds</h3>
        <% } else { %>
    <h3>You finished the quiz in <%= myMinutes%> minutes and <%= mySeconds%> seconds</h3>
    <% } %>

    <a href="/group1">Go to second round</a>

    <script src="/socket.io/socket.io.js"></script>

    <script>
        console.log('story script')
    let secondRound = document.getElementById("round2");
secondRound.addEventListener('click', lastRound);

function lastRound(){
    socket.emit('secondRound')
    location.href = "/story";
}

    </script>

    <script src="script.js"></script>

</body>
</html>